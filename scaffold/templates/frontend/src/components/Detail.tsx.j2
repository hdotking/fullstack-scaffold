import type { <<resource.name>>Response } from '../api/client'

interface <<frontend.detail_component>>Props {
  item: <<resource.name>>Response
}

export function <<frontend.detail_component>>({ item }: <<frontend.detail_component>>Props) {
  // TODO: render a detail view for all fields
  // Fields: id, <<resource_field_names>>
  return (
    <div className="bg-white p-6 rounded shadow">
      <dl className="grid grid-cols-2 gap-2 text-sm">
        {(Object.entries(item) as [string, unknown][]).map(([k, v]) => (
          <div key={k}>
            <dt className="text-gray-500">{k}</dt>
            <dd className="font-medium">{String(v)}</dd>
          </div>
        ))}
      </dl>
    </div>
  )
}
